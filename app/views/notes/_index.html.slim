- @notes.each do |note| 
  div.notes.shadow.block-message.info.alert-message
    div.page-header
          h3.title = link_to note.title, note 
    div.overflow
      p.body.ellipsis         
        = markdown(note.body)

div.pagecenter
  = paginate @notes
